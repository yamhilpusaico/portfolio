---
interface Props {
    tag: string,
    title: string,
    description: string,
    link? : string,

}
const { tag, title, description, link } = Astro.props;
---

<div class="absolute size-3 bg-gray-200 rounded-full mt-1.5 -start-1.5 border border-white dark:border-gray-900 dark:bg-gray-700"></div>
        <tag class="mb-1 text-xs md:text-sm lg:text-base font-normal leading-none text-gray-300 dark:text-gray-400">{tag}</tag>
        <h3 class="text-sm md:text-base lg:text-lg font-semibold text-gray-900 dark:text-white">{title}</h3>
        <p class="mb-4 text-xs md:text-sm lg:text-base font-normal text-gray-500 dark:text-gray-300">{description}</p>
        {
            link && (
                        <a 
                            href={link}
                            class="inline-flex items-center px-4 py-2 text-xs md:text-sm lg:text-sm font-medium text-gray-900 bg-white border border-gray-200 rounded-lg hover:bg-gray-100 hover:text-blue-700 focus:z-10 focus:ring-4 focus:outline-none focus:ring-gray-100 focus:text-blue-700 dark:bg-gray-800 dark:text-gray-400 dark:border-gray-600 dark:hover:text-white dark:hover:bg-gray-700 dark:focus:ring-gray-700"
                        >
                            Saber más... {" "}
                                <svg 
                                    class="size-3 ms-2 rtl:rotate-180" 
                                    aria-hidden="true" 
                                    xmlns="http://www.w3.org/2000/svg" 
                                    fill="none" 
                                    viewBox="0 0 14 10">
                                        <path 
                                        stroke="currentColor" 
                                        stroke-linecap="round" 
                                        stroke-linejoin="round" 
                                        stroke-width="2" d="M1 5h12m0 0L9 1m4 4L9 9"/>
                                </svg>
                        </a>
            )
        }